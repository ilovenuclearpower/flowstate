# Flowstate Runner Credentials
# Copy this file to: ~/.local/share/flowstate/runner/credentials/runner.env
# It will be auto-loaded by the nix dev shell and runner-* scripts.
#
# Alternatively, export these variables in your shell or pass them as CLI flags.

# ── Server ──
# FLOWSTATE_SERVER_URL=http://127.0.0.1:3710
# FLOWSTATE_API_KEY=

# ── Gemini (gemini-cli backend) ──
# Pick ONE auth method:
#
# Option 1: Gemini API key (simplest for dev)
# FLOWSTATE_GEMINI_API_KEY=your-api-key-here
#
# Option 2: Vertex AI service account
# FLOWSTATE_GEMINI_GCP_PROJECT=my-gcp-project
# FLOWSTATE_GEMINI_GCP_LOCATION=us-central1
#
# Option 3: Google Application Default Credentials
# Run: gcloud auth application-default login
# (no env vars needed, but requires a browser — not suitable for headless)

# ── Claude (claude-cli backend, default) ──
# Generate a long-lived token: claude setup-token
# FLOWSTATE_ANTHROPIC_AUTH_TOKEN=your-token-here
# FLOWSTATE_ANTHROPIC_BASE_URL=
# FLOWSTATE_ANTHROPIC_MODEL=

# ── OpenCode (opencode backend) ──
# FLOWSTATE_OPENCODE_PROVIDER=openrouter
# FLOWSTATE_OPENCODE_MODEL=default
# FLOWSTATE_OPENCODE_API_KEY=
# FLOWSTATE_OPENCODE_BASE_URL=
