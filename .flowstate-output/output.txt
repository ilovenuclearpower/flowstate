Implementation is complete. Here's a summary of everything that was done:

## Summary

### New Crate: `flowstate-store`
- **`src/lib.rs`** — `ObjectStore` trait with `put`, `get`, `get_opt`, `delete`, `list`, `exists` methods; `StoreError` enum; key helpers (`task_spec_key`, `task_plan_key`, `task_attachment_key`, `claude_run_prompt_key`, `claude_run_output_key`); `StoreConfig` with env var loading and `is_s3()` detection; `create_store` factory
- **`src/local.rs`** — `LocalStore` using `tokio::fs`, backwards-compatible with existing file layout, 9 unit tests
- **`src/s3.rs`** — `S3Store` using `rust-s3` 0.37 with path-style addressing, content-type detection, 10 tests (3 unit + 7 integration gated on S3 env vars)

### Modified Files
| File | Change |
|---|---|
| `Cargo.toml` (root) | Added `flowstate-store` to members, `bytes = "1"` to workspace deps |
| `flowstate-core/src/attachment.rs` | Renamed `disk_path` → `store_key` |
| `flowstate-db/src/migrations.rs` | Added v7 migration: renames column, converts absolute paths to relative keys |
| `flowstate-db/src/queries/attachments.rs` | Updated all `disk_path` references to `store_key` |
| `flowstate-server/Cargo.toml` | Added `flowstate-store`, `bytes`, `tracing` deps |
| `flowstate-server/src/lib.rs` | Creates store from env config, passes to router, logs backend type |
| `flowstate-server/src/routes/mod.rs` | Added `store: Arc<dyn ObjectStore>` to `InnerAppState` and `build_router` |
| `flowstate-server/src/routes/tasks.rs` | Replaced all `std::fs` calls with store operations |
| `flowstate-server/src/routes/claude_runs.rs` | Replaced `std::fs::read_to_string` in `get_claude_run_output` with store call |

### Validation
- `cargo clippy --workspace -- -D warnings` — **0 warnings**
- `cargo test --workspace` — **40/40 tests pass** (22 new store tests + 18 existing)
