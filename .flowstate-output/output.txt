All four phases are complete. Here's a summary of the changes made:

### Changes Summary

**3 files modified:**

1. **`crates/flowstate-core/src/task.rs`** — Added `attention_required()` method to `Task` that returns `Option<(Status, ApprovalStatus)>` indicating the earliest workflow phase needing human attention (Pending or Rejected). Added 8 unit tests covering all edge cases.

2. **`crates/flowstate-tui/src/components/task_board.rs`** — Three additions:
   - `phase_attention_indicator()` helper that maps phases to Nerd Font symbols (󰒆 Research, 󰚎 Design, 󰏗 Plan, 󰗠 Verify) with Yellow for Pending and Red for Rejected
   - Updated `render_column` to prepend the phase indicator to task titles
   - Added `select_next_attention()` method that scans all columns with wrap-around to find the next task needing attention
   - 6 unit tests for the navigation logic

3. **`crates/flowstate-tui/src/app.rs`** — Wired `N` (shift-n) key in `handle_normal()` to call `select_next_attention()`, with a status message when no tasks need attention.

### Validation
- `cargo clippy -- -D warnings` — clean, no warnings
- `cargo test --workspace --all-features` — all 28 test suites pass (0 failures)
