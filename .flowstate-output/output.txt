All three phases are complete. Here's a summary of what was implemented:

## Summary

### Files Created
- **`crates/flowstate-runner/src/backend/gemini_cli.rs`** — New `GeminiCliBackend` struct implementing the `AgentBackend` trait, with:
  - `name()` returning `"gemini-cli"`
  - `model_hint()` returning the configured model name
  - `preflight_check()` — 2-phase check: binary availability (`gemini --version`) then authentication (`gemini -p "Respond with: ok" --output-format text`)
  - `run()` — spawns `gemini -p <prompt> --output-format text --yolo` with optional `-m <model>`, delegates to `process::run_managed_with_timeout()`
  - `apply_env()` / `apply_env_async()` helpers for setting `GEMINI_API_KEY`, `GOOGLE_CLOUD_PROJECT`, `GOOGLE_CLOUD_LOCATION`, and `GOOGLE_GENAI_USE_VERTEXAI=true` (conditional on project being set)
  - 14 unit tests covering `name()`, `model_hint()` (Some/None), and all env var combination branches

### Files Modified
- **`crates/flowstate-runner/src/backend/mod.rs`** — Added `pub mod gemini_cli;`
- **`crates/flowstate-runner/src/config.rs`** — Added:
  - Import for `GeminiCliBackend`
  - 4 new config fields: `gemini_api_key`, `gemini_model`, `gemini_gcp_project`, `gemini_gcp_location`
  - `"gemini-cli"` match arm in `build_backend()`
  - Updated error message and help text to list `gemini-cli`
  - Updated `test_config()` helper
  - 2 new tests: `test_build_backend_gemini_cli` and `test_build_backend_gemini_cli_with_model`
- **`crates/flowstate-runner/tests/executor_integration.rs`** — Added 4 new `None` fields to its `test_config()` helper

### Validation Results
- **Clippy**: Zero warnings with `-D warnings`
- **Tests**: All 95 runner tests pass (77 unit + 10 executor integration + 8 workspace integration)
- **Coverage**: The `flowstate-coverage` run failed due to a pre-existing flaky `stale_runs` test in `flowstate-db` (Postgres timing issue) — completely unrelated to these changes
